#include <stdio.h>
#include <stdbool.h>

long[] return_primes(long[] num_list);
bool is_prime(long x);

int main(void)
{
    return 0;
}


long[] return_primes(long[] num_list)
{
    for (size_t i = 0; i < sizeof(prime_numbers); i++)
    {
        if (is_prime(i))
        {
            printf("\n")
        }
    }
}

bool is_prime(long x)
{
    return 0;
}

    
